#include <iostream>
using namespace std;
int main()
{
    setlocale(0, "");
    int cheslo;
    cout << "Введите число: ";
    cin >> cheslo;
    for (int i = cheslo; i > 0; --i)
    {
        if (cheslo%i==0) {
            cout <<"делитель: "<< i<<endl;
        }
    }
}
